<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.petrus.asus.sportrnd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".activity_splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_daftar" />
        <activity android:name=".activity_halamanutama" />
        <activity android:name=".activity_help" />
        <activity
            android:name=".Aboutactivity"
            android:label="About" />
        <activity android:name=".activity_lapangan" />
        <activity android:name=".activity_forgetpass"></activity>
        <activity android:name=".activity_football"/>
        <activity android:name=".activity_basket"></activity>
    </application>

</manifest>